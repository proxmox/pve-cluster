#!/bin/sh

set -e

case "$1" in
  configure)
    # TODO: remove with PVE 10+
    if dpkg --compare-versions "$2" 'lt' '8.1.3'; then
      [ -d /var/lib/pve-cluster/backup ] && chmod -v 0700 /var/lib/pve-cluster/backup
    fi
  ;;
esac

#DEBHELPER#

exit 0

